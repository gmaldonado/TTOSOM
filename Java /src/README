To compile you should use:

javac -cp ../lib/weka.jar:. *.java

To run you should use:

java -cp ../lib/weka.jar:. Main <parameters>

Example:

java -cp ../lib/weka.jar:. Main ../iris.arff ../topology.txt 1 2 0 2 0 0
